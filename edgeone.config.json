{
  "name": "theme-market-cn",
  "version": "1.0.0",
  "build": {
    "command": "npm run build",
    "output": "./dist"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}